{
  "name": "klar-lag1201",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/app.js && node --check src/questions.js && node --check src/grading.js"
  },
  "engines": {
    "node": ">=20"
  }
}
